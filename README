-*- markdown -*-


XRay -- Coding Analysis for SCMs
================================


Patches can be submitted via:

- preferrably creating an account at Bitbucket and forking
  [xray project](http://bitbucket.org/balena/xray/).
- [Sourceforge bugtracking](https://sourceforge.net/projects/xray/).
- mail patches to guibv att comunip.com.br
- or work directly on hg, if you got write access
  ssh://xray.hg.sourceforge.net/hgroot/xray/xray


Requirements:
-------------

Yes, there are some dependencies to check before installing XRay:

- cmdln >= 1.1.2 for fancy command line parsing
- SQLObject >= 0.12.0 for generic access to databases
- pysvn for support to SVN repositories
- progressbar >= 2.2 for progress bar and ETA
- pydot >= 1.0.2 for generating useful repository graphs
- matplotlib >= 0.99.1.1 for reporting charts
- PyYAML >= 3.09 for abstract chart configurations and output
- ohcount Python extension >= 3.0.0
  available at http://github.com/balena/ohcount


Installation:
-------------

To install from hg or from source package, do:

    python setup.py build
    python setup.py install


Usage:
------

To create a new XRay base, you will need a new empty directory:

    $ mkdir repos
    $ cd repos
    $ xray init

It is possible to delegate database operations to a centralized one:

    $ xray init --db=mysql://user:password@host/database 

Then, you simply add a bunch of repositories:

    $ xray addrepos svn http://some.domain/path/to/repos

XRay imports metadata from SCM repositories to correlate data, execute faster
queries and mix data between repositories. To synchronize repositories metadata
execute (take a time for a coffee):

    $ xray sync

Whenever you need to create reports, do:

    $ xray report

Have fun!


Authors:
--------
Guilherme Balena Versiani - guibv at comunip dot com dot br
